{
	"name": "@world-issue-tracker/api",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "wrangler dev",
		"build": "wrangler deploy --dry-run --outdir=dist",
		"check": "tsc --noEmit",
		"lint": "biome check .",
		"test": "vitest run"
	},
	"dependencies": {
		"@world-issue-tracker/shared": "workspace:*",
		"hono": "^4.7.0"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.0",
		"@cloudflare/workers-types": "^4.20250214.0",
		"vitest": "~3.0.0",
		"wrangler": "^4.0.0"
	}
}
