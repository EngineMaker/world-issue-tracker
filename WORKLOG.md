# 作業ログ — world-issue-tracker（WIT）

> **迷ったらここだけ読めば戻れます。** 詳細は下に追記式で続きます。

## 🚨 2026-09-23 — 本番デプロイが 8/22 から失敗し続けている

**main への push で走る Deploy ワークフローが、2026-08-22 以降ずっと失敗している。**
（8/22 に2回、8/23、9/23 と4回連続で failure。CI 自体は success）

**原因**: `wrangler` が Cloudflare API に弾かれている。

```
KV namespace '0000000000000000000000000000000000' is not valid.
Please verify the namespace_id in your configuration.
```

**KV namespace の ID がプレースホルダ（ゼロ埋め）のまま**で、実在の値が入っていない。
コードの問題ではなく**設定の問題**。

### ⚠️ これが意味すること

**本番（https://issues.emaker.dev ）は 8/22 時点のコードで動いたまま。**
それ以降にマージしたもの（**PR #160 の Issue 編集機能**を含む）は
**本番に反映されていない**可能性が高い。

### 👉 直すには

1. Cloudflare ダッシュボードで実際の KV namespace ID を確認する
2. `apps/api/wrangler.toml`（または該当する設定ファイル）の `namespace_id` を差し替える
   - リポジトリに直接書くか、GitHub Secrets 経由にするかは要判断
3. 再デプロイして、本番に PR #160 の内容が出ているか確認する

## 🔖 いまの状況（2026-09-21 時点）

**この現場は何**: **「地球のバグを、みんなで可視化して、みんなで直す」**。
ソフトウェアの Issue Tracker を現実世界に持ち込み、個人の困りごとから国際問題までを
**個人 → コミュニティ → 自治体 → 国 → 世界** の5スコープで扱う。

> **裏テーマ: 隣り合う Issue が視野を広げる。blame ではなく fix。対立ではなく issue。**

個人の悩みと世界の課題を地続きに見せる、というのが他の要望投稿サイトとの違い。

**🟢 本番稼働中**: https://issues.emaker.dev
（API: `world-issue-tracker-api.mktoho.workers.dev`）

**壊れているところは無い。オープンな不具合もゼロ。**
残っているのは**2件の意思決定**だけ（下記）。

### 👉 次にやること

**手を動かす前に、決めることが2つある。** どちらも「実装が難しい」のではなく
**「どうするか決めかねている」**ので止まっている。

#### 🤔 #70 — 裏テーマを実装する方法を選ぶ

「隣り合う Issue が視野を広げる」を、どういう形で実現するか。**思想の選択**。

| 案 | 重さ | 効果 |
|---|---|---|
| 同じカテゴリを並べる | 軽 | 薄い |
| 近い場所の Issue を見せる | 中 | — |
| **個人 → 自治体 → 国 を繋いで見せる** | 重 | **裏テーマそのもの** |
| 同種が集まったらスコープ引き上げを提案 | 最重 | 判定精度勝負 |

**自分で決めた結論**: リアクション（#112「私も困っている」の数）が入って、
**件数が溜まってから再判断する**。→ まだそのときではない。

#### 💰 #66 — 本文の自動翻訳、コスト上限を決める

UI の多言語化（#82）とは別で、**Issue 本文を LLM で翻訳する**話。
**このサービス初の従量課金機能**なので、決めることが4つある。

1. いつ翻訳するか（投稿時に保存 / 閲覧時に都度）
2. **原文と訳文の両方を持つか**（誤訳のとき本人の言葉に辿れるか）
3. 失敗したときどうするか
4. **月額いくらまで**

**自分で決めた結論**: 上限を決めてから着手。いま日本語話者しか使っていないので急がない。

#### 📄 未コミットの2ファイル

`docs/design-handoff.md`（デザイン改修の指示書）と
`docs/world-issue-tracker-redesign.html`（モック）。

**中身の作業はすべて完了済み**（フェーズA=PR #110、フェーズB=PR #111、8/17 マージ）。
成果物だけコミットされて、指示書が手元に残った形。
**経緯の資料として `docs/` に素直にコミットしてよい。**

> ⚠️ ただしモック内の「先月、隣の空き地の草が刈られました」は**架空の解決事例**。
> 指示書自身が「絶対にそのまま公開するな」と警告している。
> 指示書 §3 の「濃緑バンドに解決事例を載せる」案は、
> **解決済み Issue がまだ1件も無い**ため未決着。

## 🏗 どう出来ているか

| | |
|---|---|
| 構成 | TypeScript / Turborepo のモノレポ |
| API | Hono → Cloudflare Workers |
| Web | Next.js App Router → Cloudflare Workers（OpenNext） |
| DB | D1（マイグレーション10本） |
| 認証 | Clerk |
| その他 | Zod で型共有 / Biome / Vitest |
| デプロイ | main に push で GitHub Actions が自動。本番 Clerk キー（pk_live）を検査 |

**208コミット / TS・TSX 151ファイル・約4万行 / テスト61ファイル。**
コメント・応援表明・写真・匿名投稿・リアクション・位置情報の粗化まで実装済み。

本番のデータは実質4件程度。**解決済みの Issue はまだゼロ。**

## 🔗 loop-engineering との関係

`~/work/ai/loop-engineering` の**3つのループが、このリポジトリを題材に回っている**
（Issue を見つけて起票し、直して PR を出す）。

**いま修正ループは毎回 SKIP している。** 処理できる Issue が `needs-discussion` の
**#70 / #66 しか無いため**。つまり **上の2つを決めることが、ループの燃料になる。**

## ⚠️ 触るとき気をつけること

README に運用の落とし穴が書いてある。特に:

- **本番にテスト起票をしない**（誤投入したデータの消し方も README にある）
- Clerk の本番キー、`NEXT_PUBLIC_API_URL` の二重管理

---

### 📌 2026-09-21 — 記録を起こした

HQ の棚卸しで「記録がない現場」として挙がったため WORKLOG を作成。
未コミット2ファイルの正体（実装済みの指示書とモック）と、
#70 / #66 が保留されている**理由**をここに残した。
